<% content_for(:page_title, "Workflow Definitions") %>

<div class="flex items-center justify-between mb-6">
  <h2 class="text-xl font-semibold text-gray-100">Workflow Definitions</h2>
  <%= link_to new_workflow_definition_path, class: "inline-flex items-center px-4 py-2 bg-red-600 hover:bg-red-700 text-white rounded-lg text-sm font-medium transition-colors" do %>
    <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"/>
    </svg>
    New Workflow
  <% end %>
</div>

<div class="bg-gray-800 border border-gray-700 rounded-lg overflow-hidden">
  <table class="min-w-full divide-y divide-gray-700">
    <thead class="bg-gray-800">
      <tr>
        <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-400 uppercase tracking-wider">Name</th>
        <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-400 uppercase tracking-wider">Slug</th>
        <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-400 uppercase tracking-wider">States</th>
        <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-400 uppercase tracking-wider">Target Type</th>
        <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-400 uppercase tracking-wider">Created</th>
        <th scope="col" class="px-6 py-3 text-right text-xs font-medium text-gray-400 uppercase tracking-wider">Actions</th>
      </tr>
    </thead>
    <tbody id="workflow_definitions" class="divide-y divide-gray-700">
      <% @workflow_definitions.each do |workflow_definition| %>
        <%= render workflow_definition %>
      <% end %>
    </tbody>
  </table>

  <% if @workflow_definitions.empty? %>
    <div class="px-6 py-12 text-center">
      <p class="text-gray-400">No workflow definitions found.</p>
      <%= link_to "Create your first workflow", new_workflow_definition_path, class: "text-red-400 hover:text-red-300 text-sm mt-2 inline-block" %>
    </div>
  <% end %>
</div>
