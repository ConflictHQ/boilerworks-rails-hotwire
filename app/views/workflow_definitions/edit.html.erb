<% content_for(:page_title, "Edit #{@workflow_definition.name}") %>

<div class="mb-6">
  <%= link_to workflow_definition_path(@workflow_definition.uuid), class: "inline-flex items-center text-sm text-gray-400 hover:text-gray-200" do %>
    <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/>
    </svg>
    Back to Workflow
  <% end %>
</div>

<div class="bg-gray-800 border border-gray-700 rounded-lg p-6">
  <h2 class="text-xl font-semibold text-gray-100 mb-6">Edit Workflow Definition</h2>
  <%= render "form", workflow_definition: @workflow_definition %>
</div>
